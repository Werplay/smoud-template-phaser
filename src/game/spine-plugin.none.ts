/**
 * The stand-in written over spine-plugin.ts when a project has no skeleton in
 * it. Nothing imports the package, so nothing of it is in the build.
 */
export const SPINE_PLUGIN: unknown = null;
